# NIMBUS Environment Variables Configuration

# PostgreSQL Connection String (PostGIS enabled)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/nimbus

# Flag to enable static/mock fallback datasets when external APIs are offline
DEMO_MODE=true
